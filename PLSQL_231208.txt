PL/SQL

SELECT
DML : INSERT, UPDATE, DELETE
DDL : CREATE, ALTER, DROP
		ALTER(구조변경) --> ADD, MODIFY(not null/default), DROP
		>> ALTER 객체 객체이름
		>> ADD 컬럼/제약조건 컬럼명/제약조건명
DCL : GRANT, REVOKE
TCL : COMMIT, ROLLBACK, SAVEPOINT


트랜잭션

서브쿼리
단일행/다중행

https://docs.oracle.com/en/database/index.html

--------------------------------------------------------------------------------------------

SQL_Developer 영어로 바꾸기 >>
D:\db\sqldeveloper-22.2.1.234.1810-x64\sqldeveloper\sqldeveloper\bin 폴더찾아가서 : sqldeveloper.conf열기
아래줄에 추가 : AddVMOption -Duser.language=en

--------------------------------------------------------------------------------------------

속도면에서 따지면 char가 동일한 사이즈에 대응해서 더 빠르다
메모리 효율에서는 varchar2가 더 나음

100 char : 영문이든 한글이든 무조건 100글자

--------------------------------------------------------------------------------------------

BEGIN
    DBMS_OUTPUT.PUT_LINE(TO_CHAR(sysdate, 'yyyy"년"MM"월"dd"일"'));
    --이렇게 단독으로 들어가는 함수, 그룹함수는 의미가 없다
    COUNT(1);
END;
/

끝날 때 '/' 붙인다
'/'가 없으면 정상적으로 블록을 잡지를 못해서 이상하게 수행한다
END;/ >> 이렇게 하면 오타로 인식
무조건 독립된 한줄로 들어가서 끊어내야 한다

--------------------------------------------------------------------------------------------

블록이 끝나는 것과 트랜잭션이 끝나는 것은 상관이 없다
내부 블록은 바깥에 있는 블록에 손을 댈 수 있지만,
바깥에 있는 블록은 내부 블록에 손도 댈 수 없다.
중첩블록 >> 변수의 범위에 대해서 생각하기

--------------------------------------------------------------------------------------------

접속하면 먼저,
>> SET SERVEROUTPUT ON 실행

SELECT문을 쓰려면 무조건 변수가 필요함 >> DECLARE절이 들어간다

--------------------------------------------------------------------------------------------

ROWCOUNT
커서
IF / LOOP
조건 : 위에서부터 주욱... 하다가 잘라낸다고 생각

--------------------------------------------------------------------------------------------

